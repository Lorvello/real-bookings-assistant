# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env
.env.local

# TypeScript incremental build cache (from `tsc -b` type-check gate)
*.tsbuildinfo

# Launch-ready-loop sim scratch dirs: throwaway harness drivers, evidence PNGs,
# and the Deno lockfile, kept local-only so they never land in this PUBLIC repo.
_automation/
evidence/
deno.lock

# Supabase CLI local-state artifacts (project-ref, version pins, pooler-url, etc.):
# regenerated by the CLI, never source. F-V06.
supabase/.temp/
